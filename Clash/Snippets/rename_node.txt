;FlowerCloud
(IEPL|专线|中继)@

;奈云
(\w+)\|(.*)\|\w+(\s+(.*))@$2 - $1

;去除重复空格
\s+(\s)@$1

;移除字符(|)
\|@

;移除倍率
;\s?\(?((x|X)?(\d+)(\.?\d+)?)((\s?倍率?)|(x|X))\)?|(\*\d+\.?\d+?)?|(x|X)\d?\d(\.\d)?@

;移除速率
?(\d)+(G|M)(bps)?@
